[project]
name = "nova-sdk"
version = "0.1.0"
description = "The runtime Nova modules are written against: manifest, arguments, protocol."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sdk"]
